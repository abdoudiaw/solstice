# Examples

`quickstart.ipynb` (coming with the first weight release): install
solps-nn, `zoo.load()` a released bundle, predict a held-out case, plot
Te/ne on the mesh. Designed to run on a free Colab GPU — it is the
acceptance test for the whole inference path.
